<UserControl
    x:Class="Employee_Management_System.UserControls.Reports"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="700"
    d:DesignWidth="1200"
    Background="#0F172A"
    mc:Ignorable="d">

    <UserControl.Resources>
        <!-- Section Header Style -->
        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="22" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="#F8FAFC" />
            <Setter Property="Margin" Value="0,0,0,20" />
        </Style>

        <!-- Label Style -->
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="#CBD5E1" />
            <Setter Property="Margin" Value="0,0,0,8" />
            <Setter Property="FontWeight" Value="Medium" />
        </Style>

        <!-- ComboBox Style -->
        <Style x:Key="DarkComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#1E293B" />
            <Setter Property="Foreground" Value="#F8FAFC" />
            <Setter Property="BorderBrush" Value="#475569" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border
                                x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter
                                        Grid.Column="0"
                                        Margin="{TemplateBinding Padding}"
                                        HorizontalAlignment="Left"
                                        VerticalAlignment="Center"
                                        Content="{TemplateBinding SelectionBoxItem}"
                                        ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" />
                                    <ToggleButton
                                        Grid.Column="1"
                                        Width="30"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        ClickMode="Press"
                                        Focusable="False"
                                        IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                        <Path
                                            Width="10"
                                            Height="6"
                                            Data="M 0 0 L 5 5 L 10 0 Z"
                                            Fill="#94A3B8"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center" />
                                    </ToggleButton>
                                </Grid>
                            </Border>
                            <Popup
                                x:Name="Popup"
                                AllowsTransparency="True"
                                Focusable="False"
                                IsOpen="{TemplateBinding IsDropDownOpen}"
                                Placement="Bottom"
                                PopupAnimation="Slide">
                                <Border
                                    Margin="0,4,0,0"
                                    Background="#1E293B"
                                    BorderBrush="#475569"
                                    BorderThickness="1"
                                    CornerRadius="6">
                                    <Border.Effect>
                                        <DropShadowEffect
                                            BlurRadius="12"
                                            Direction="270"
                                            Opacity="0.5"
                                            ShadowDepth="4"
                                            Color="Black" />
                                    </Border.Effect>
                                    <ScrollViewer MaxHeight="200" VerticalScrollBarVisibility="Auto">
                                        <ItemsPresenter />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#3B82F6" />
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#3B82F6" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBoxItem Style -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#F8FAFC" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border
                            x:Name="Border"
                            Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#475569" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#334155" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DatePicker Style -->
        <Style x:Key="DarkDatePickerStyle" TargetType="DatePicker">
            <Setter Property="Background" Value="#1E293B" />
            <Setter Property="Foreground" Value="#F8FAFC" />
            <Setter Property="BorderBrush" Value="#475569" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="CalendarStyle">
                <Setter.Value>
                    <Style TargetType="Calendar">
                        <Setter Property="Background" Value="#1E293B" />
                        <Setter Property="BorderBrush" Value="#475569" />
                        <Setter Property="Foreground" Value="#F8FAFC" />
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DatePicker">
                        <Border
                            x:Name="Border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <DatePickerTextBox
                                    x:Name="PART_TextBox"
                                    Grid.Column="0"
                                    Padding="12,0,0,0"
                                    VerticalContentAlignment="Center"
                                    Background="Transparent"
                                    Foreground="#F8FAFC"
                                    FontSize="13" />
                                <Button
                                    x:Name="PART_Button"
                                    Grid.Column="1"
                                    Width="36"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Focusable="False">
                                    <Path
                                        Width="14"
                                        Height="14"
                                        Data="M19,4H18V2H16V4H8V2H6V4H5C3.89,4 3,4.9 3,6V20A2,2 0 0,0 5,22H19A2,2 0 0,0 21,20V6A2,2 0 0,0 19,4M19,20H5V10H19V20M5,8V6H19V8H5M7,12H12V17H7V12Z"
                                        Fill="#94A3B8"
                                        Stretch="Uniform" />
                                </Button>
                                <Popup
                                    x:Name="PART_Popup"
                                    AllowsTransparency="True"
                                    Placement="Bottom"
                                    PlacementTarget="{Binding ElementName=Border}"
                                    StaysOpen="False" />
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#3B82F6" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DatePickerTextBox Style -->
        <Style TargetType="DatePickerTextBox">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#F8FAFC" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="CaretBrush" Value="#F8FAFC" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DatePickerTextBox">
                        <Grid>
                            <ScrollViewer
                                x:Name="PART_ContentHost"
                                Padding="{TemplateBinding Padding}"
                                VerticalAlignment="Center"
                                Background="{TemplateBinding Background}"
                                Foreground="{TemplateBinding Foreground}" />
                            <TextBlock
                                x:Name="PART_Watermark"
                                Padding="12,0,0,0"
                                VerticalAlignment="Center"
                                Foreground="#94A3B8"
                                IsHitTestVisible="False"
                                Text="Select date..."
                                Visibility="Collapsed" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="PART_Watermark" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <Trigger Property="Text" Value="{x:Null}">
                                <Setter TargetName="PART_Watermark" Property="Visibility" Value="Visible" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Calendar Style -->
        <Style TargetType="CalendarItem">
            <Setter Property="Background" Value="#1E293B" />
            <Setter Property="BorderBrush" Value="#475569" />
        </Style>

        <Style TargetType="CalendarDayButton">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#F8FAFC" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CalendarDayButton">
                        <Border
                            x:Name="Border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="Transparent"
                            BorderThickness="1">
                            <ContentPresenter
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#475569" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#3B82F6" />
                                <Setter Property="Foreground" Value="#F8FAFC" />
                            </Trigger>
                            <Trigger Property="IsToday" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#3B82F6" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="CalendarButton">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#F8FAFC" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CalendarButton">
                        <Border
                            x:Name="Border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="Transparent"
                            BorderThickness="1">
                            <ContentPresenter
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#475569" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary Button Style -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3B82F6" />
            <Setter Property="Foreground" Value="#F8FAFC" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="24,10" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            x:Name="Border"
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#2563EB" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Background" Value="#64748B" />
                                <Setter Property="Cursor" Value="Arrow" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary Button Style -->
        <Style
            x:Key="SecondaryButtonStyle"
            BasedOn="{StaticResource PrimaryButtonStyle}"
            TargetType="Button">
            <Setter Property="Background" Value="#64748B" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            x:Name="Border"
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#475569" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Background" Value="#334155" />
                                <Setter Property="Cursor" Value="Arrow" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DataGrid Style -->
        <Style x:Key="DataGridStyle" TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False" />
            <Setter Property="IsReadOnly" Value="True" />
            <Setter Property="SelectionMode" Value="Single" />
            <Setter Property="CanUserAddRows" Value="False" />
            <Setter Property="CanUserDeleteRows" Value="False" />
            <Setter Property="HeadersVisibility" Value="Column" />
            <Setter Property="GridLinesVisibility" Value="None" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="#475569" />
            <Setter Property="Background" Value="#1E293B" />
            <Setter Property="RowBackground" Value="#1E293B" />
            <Setter Property="AlternatingRowBackground" Value="#334155" />
            <Setter Property="Foreground" Value="#F8FAFC" />
            <Setter Property="RowHeight" Value="40" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="ColumnHeaderHeight" Value="44" />
        </Style>

        <!-- DataGrid Header Style -->
        <Style x:Key="DataGridHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#334155" />
            <Setter Property="Foreground" Value="#F8FAFC" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Padding" Value="12,10" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="BorderBrush" Value="#475569" />
            <Setter Property="BorderThickness" Value="0,0,1,0" />
        </Style>

        <!-- DataGrid Cell Style -->
        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#475569" />
                    <Setter Property="Foreground" Value="#F8FAFC" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- DataGrid Row Style -->
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#475569" />
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#475569" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ScrollBar Styles -->
        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="#1E293B" />
            <Setter Property="BorderBrush" Value="#334155" />
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="7">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="24" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="24" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!--  SECTION 1: Monthly Attendance Report  -->
            <Border
                Grid.Row="0"
                Padding="18"
                Background="#334155"
                BorderBrush="#475569"
                BorderThickness="1"
                CornerRadius="8">
                <Border.Effect>
                    <DropShadowEffect
                        BlurRadius="12"
                        Direction="270"
                        Opacity="0.4"
                        ShadowDepth="4"
                        Color="Black" />
                </Border.Effect>

                <StackPanel>
                    <!--  Header  -->
                    <TextBlock Style="{StaticResource SectionHeaderStyle}" Text="📊 Monthly Attendance Report" />

                    <!--  Filter Section  -->
                    <Grid Margin="0,0,0,24">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="20" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="20" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="20" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <!--  Month  -->
                        <StackPanel Grid.Column="0">
                            <TextBlock Style="{StaticResource LabelStyle}" Text="Month:" />
                            <ComboBox
                                Height="40"
                                ItemsSource="{Binding Months}"
                                SelectedItem="{Binding SelectedMonth}"
                                Style="{StaticResource DarkComboBoxStyle}" />
                        </StackPanel>

                        <!--  Year  -->
                        <StackPanel Grid.Column="2">
                            <TextBlock Style="{StaticResource LabelStyle}" Text="Year:" />
                            <ComboBox
                                Height="40"
                                ItemsSource="{Binding Years}"
                                SelectedItem="{Binding SelectedYear}"
                                Style="{StaticResource DarkComboBoxStyle}" />
                        </StackPanel>

                        <!--  Employee  -->
                        <StackPanel Grid.Column="4">
                            <TextBlock Style="{StaticResource LabelStyle}" Text="Employee:" />
                            <ComboBox
                                Height="40"
                                DisplayMemberPath="FullName"
                                ItemsSource="{Binding Employees}"
                                SelectedValue="{Binding SelectedEmployeeId}"
                                SelectedValuePath="EmployeeId"
                                Style="{StaticResource DarkComboBoxStyle}" />
                        </StackPanel>

                        <!--  Generate Button  -->
                        <StackPanel Grid.Column="6" VerticalAlignment="Bottom">
                            <Button
                                Width="160"
                                Height="40"
                                Command="{Binding GenerateAttendanceReportCommand}"
                                Content="Generate Report"
                                Style="{StaticResource PrimaryButtonStyle}" />
                        </StackPanel>
                    </Grid>

                    <!--  Results DataGrid  -->
                    <Border
                        BorderBrush="#475569"
                        BorderThickness="1"
                        CornerRadius="8">
                        <DataGrid
                            MaxHeight="320"
                            ColumnHeaderStyle="{StaticResource DataGridHeaderStyle}"
                            ItemsSource="{Binding AttendanceReports}"
                            Style="{StaticResource DataGridStyle}">
                            <DataGrid.Columns>
                                <DataGridTextColumn
                                    Width="Auto"
                                    Binding="{Binding EmployeeId}"
                                    Header="Employee ID" />
                                <DataGridTextColumn
                                    Width="*"
                                    Binding="{Binding EmployeeName}"
                                    Header="Employee Name" />
                                <DataGridTextColumn
                                    Width="*"
                                    Binding="{Binding Department}"
                                    Header="Department" />
                                <DataGridTextColumn
                                    Width="*"
                                    Binding="{Binding Designation}"
                                    Header="Designation" />
                                <DataGridTextColumn
                                    Width="90"
                                    Binding="{Binding TotalPresentDays}"
                                    Header="Present" />
                                <DataGridTextColumn
                                    Width="90"
                                    Binding="{Binding TotalAbsentDays}"
                                    Header="Absent" />
                                <DataGridTextColumn
                                    Width="90"
                                    Binding="{Binding TotalHalfDays}"
                                    Header="Half Days" />
                                <DataGridTextColumn
                                    Width="80"
                                    Binding="{Binding LateCheckIns}"
                                    Header="Late" />
                                <DataGridTextColumn
                                    Width="100"
                                    Binding="{Binding AverageWorkingHours, StringFormat=F2}"
                                    Header="Avg Hours" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>

                    <!--  Export Button  -->
                    <Button
                        Width="160"
                        Height="40"
                        Margin="0,20,0,0"
                        HorizontalAlignment="Right"
                        Command="{Binding ExportAttendanceToCsvCommand}"
                        Content="📥 Export to CSV"
                        Style="{StaticResource SecondaryButtonStyle}" />
                </StackPanel>
            </Border>

            <!--  SECTION 2: Leave Summary by Department  -->
            <Border
                Grid.Row="2"
                Padding="18"
                Background="#334155"
                BorderBrush="#475569"
                BorderThickness="1"
                CornerRadius="8">
                <Border.Effect>
                    <DropShadowEffect
                        BlurRadius="12"
                        Direction="270"
                        Opacity="0.4"
                        ShadowDepth="4"
                        Color="Black" />
                </Border.Effect>

                <StackPanel>
                    <!--  Header  -->
                    <TextBlock Style="{StaticResource SectionHeaderStyle}" Text="📋 Leave Summary by Department" />

                    <!--  Filter Section  -->
                    <Grid Margin="0,0,0,24">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="20" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="20" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="20" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <!--  Start Date  -->
                        <StackPanel Grid.Column="0">
                            <TextBlock Style="{StaticResource LabelStyle}" Text="Start Date:" />
                            <DatePicker
                                Height="40"
                                SelectedDate="{Binding StartDate}"
                                Style="{StaticResource DarkDatePickerStyle}" />
                        </StackPanel>

                        <!--  End Date  -->
                        <StackPanel Grid.Column="2">
                            <TextBlock Style="{StaticResource LabelStyle}" Text="End Date:" />
                            <DatePicker
                                Height="40"
                                SelectedDate="{Binding EndDate}"
                                Style="{StaticResource DarkDatePickerStyle}" />
                        </StackPanel>

                        <!--  Department  -->
                        <StackPanel Grid.Column="4">
                            <TextBlock Style="{StaticResource LabelStyle}" Text="Department:" />
                            <ComboBox
                                Height="40"
                                ItemsSource="{Binding Departments}"
                                SelectedItem="{Binding SelectedDepartment}"
                                Style="{StaticResource DarkComboBoxStyle}" />
                        </StackPanel>

                        <!--  Generate Button  -->
                        <StackPanel Grid.Column="6" VerticalAlignment="Bottom">
                            <Button
                                Width="160"
                                Height="40"
                                Command="{Binding GenerateLeaveSummaryCommand}"
                                Content="Generate Report"
                                Style="{StaticResource PrimaryButtonStyle}" />
                        </StackPanel>
                    </Grid>

                    <!--  Results DataGrid  -->
                    <Border
                        BorderBrush="#475569"
                        BorderThickness="1"
                        CornerRadius="8">
                        <DataGrid
                            MaxHeight="320"
                            ColumnHeaderStyle="{StaticResource DataGridHeaderStyle}"
                            ItemsSource="{Binding LeaveSummaryReports}"
                            Style="{StaticResource DataGridStyle}">
                            <DataGrid.Columns>
                                <DataGridTextColumn
                                    Width="*"
                                    Binding="{Binding Department}"
                                    Header="Department" />
                                <DataGridTextColumn
                                    Width="130"
                                    Binding="{Binding TotalLeaveRequests}"
                                    Header="Total Requests" />
                                <DataGridTextColumn
                                    Width="110"
                                    Binding="{Binding ApprovedLeaves}"
                                    Header="Approved" />
                                <DataGridTextColumn
                                    Width="110"
                                    Binding="{Binding RejectedLeaves}"
                                    Header="Rejected" />
                                <DataGridTextColumn
                                    Width="110"
                                    Binding="{Binding PendingLeaves}"
                                    Header="Pending" />
                                <DataGridTextColumn
                                    Width="160"
                                    Binding="{Binding MostCommonLeaveType}"
                                    Header="Most Common Type" />
                                <DataGridTextColumn
                                    Width="110"
                                    Binding="{Binding TotalLeaveDays}"
                                    Header="Total Days" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>

                    <!--  Export Button  -->
                    <Button
                        Width="160"
                        Height="40"
                        Margin="0,20,0,0"
                        HorizontalAlignment="Right"
                        Command="{Binding ExportLeaveSummaryToCsvCommand}"
                        Content="📥 Export to CSV"
                        Style="{StaticResource SecondaryButtonStyle}" />
                </StackPanel>
            </Border>

            
        </Grid>
    </ScrollViewer>
</UserControl>