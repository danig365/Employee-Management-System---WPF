<UserControl x:Class="Employee_Management_System.UserControls.MyAttendanceControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             Background="White">

    <UserControl.Resources>
        <Style x:Key="FilterButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2980B9"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="StatCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#ECF0F1"/>
            <Setter Property="CornerRadius" Value="5"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="MinWidth" Value="150"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" 
                   Text="My Attendance Records" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   Foreground="#2C3E50"
                   Margin="0,0,0,20"/>

        <!-- Filter Section -->
        <Border Grid.Row="1" 
                Background="White" 
                BorderBrush="#BDC3C7" 
                BorderThickness="1"
                CornerRadius="5"
                Padding="15"
                Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" 
                           Text="From:" 
                           VerticalAlignment="Center"
                           FontWeight="SemiBold"
                           Margin="0,0,10,0"/>

                <DatePicker x:Name="StartDatePicker" 
                            Grid.Column="1"
                            Margin="0,0,15,0"
                            VerticalAlignment="Center"/>

                <TextBlock Grid.Column="2" 
                           Text="To:" 
                           VerticalAlignment="Center"
                           FontWeight="SemiBold"
                           Margin="0,0,10,0"/>

                <DatePicker x:Name="EndDatePicker" 
                            Grid.Column="3"
                            Margin="0,0,15,0"
                            VerticalAlignment="Center"/>

                <StackPanel Grid.Column="4" Orientation="Horizontal">
                    <Button Content="🔍 Filter" 
                            Style="{StaticResource FilterButtonStyle}"
                            Click="FilterButton_Click"/>
                    <Button Content="🔄 Reset" 
                            Style="{StaticResource FilterButtonStyle}"
                            Click="ResetButton_Click"
                            Background="#95A5A6"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Statistics Cards -->
        <Grid Grid.Row="2" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="Total Present" 
                               FontSize="11" 
                               Foreground="#7F8C8D"
                               Margin="0,0,0,5"/>
                    <TextBlock x:Name="TotalPresentText" 
                               Text="0" 
                               FontSize="24" 
                               FontWeight="Bold"
                               Foreground="#27AE60"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="Total Absent" 
                               FontSize="11" 
                               Foreground="#7F8C8D"
                               Margin="0,0,0,5"/>
                    <TextBlock x:Name="TotalAbsentText" 
                               Text="0" 
                               FontSize="24" 
                               FontWeight="Bold"
                               Foreground="#E74C3C"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="Total Half-Days" 
                               FontSize="11" 
                               Foreground="#7F8C8D"
                               Margin="0,0,0,5"/>
                    <TextBlock x:Name="TotalHalfDaysText" 
                               Text="0" 
                               FontSize="24" 
                               FontWeight="Bold"
                               Foreground="#F39C12"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="3" Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="Avg Hours" 
                               FontSize="11" 
                               Foreground="#7F8C8D"
                               Margin="0,0,0,5"/>
                    <TextBlock x:Name="AvgHoursText" 
                               Text="0.00" 
                               FontSize="24" 
                               FontWeight="Bold"
                               Foreground="#3498DB"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Attendance DataGrid -->
        <Border Grid.Row="3" 
                Background="White" 
                BorderBrush="#BDC3C7" 
                BorderThickness="1"
                CornerRadius="5"
                Padding="10">
            <DataGrid x:Name="AttendanceDataGrid"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      Background="White"
                      BorderThickness="0"
                      RowHeight="45"
                      AlternatingRowBackground="#F8F9FA">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Date" 
                                        Binding="{Binding AttendanceDate, StringFormat='{}{0:MMM dd, yyyy}'}" 
                                        Width="*"
                                        FontWeight="SemiBold"/>
                    <DataGridTextColumn Header="Day" 
                                        Binding="{Binding AttendanceDate, StringFormat='{}{0:dddd}'}" 
                                        Width="*"/>
                    <DataGridTextColumn Header="Check In" 
                                        Binding="{Binding CheckInDisplay}" 
                                        Width="*"
                                        Foreground="#27AE60"
                                        FontWeight="SemiBold"/>
                    <DataGridTextColumn Header="Check Out" 
                                        Binding="{Binding CheckOutDisplay}" 
                                        Width="*"
                                        Foreground="#E74C3C"
                                        FontWeight="SemiBold"/>
                    <DataGridTextColumn Header="Total Hours" 
                                        Binding="{Binding TotalHours, StringFormat='{}{0:F2} hrs'}" 
                                        Width="*"
                                        FontWeight="Bold"/>
                    <DataGridTemplateColumn Header="Status" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="3" 
                                        Padding="8,4"
                                        HorizontalAlignment="Center">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Status}" Value="Present">
                                                    <Setter Property="Background" Value="#D5F4E6"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="Absent">
                                                    <Setter Property="Background" Value="#FADBD8"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="Half-day">
                                                    <Setter Property="Background" Value="#FCF3CF"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding Status}" 
                                               FontWeight="SemiBold"
                                               HorizontalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="Present">
                                                        <Setter Property="Foreground" Value="#27AE60"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Absent">
                                                        <Setter Property="Foreground" Value="#E74C3C"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Half-day">
                                                        <Setter Property="Foreground" Value="#F39C12"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>