<UserControl x:Class="Employee_Management_System.UserControls.EmployeeAttendanceControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             Background="White">

    <UserControl.Resources>
        <Style x:Key="InfoCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#ECF0F1"/>
            <Setter Property="CornerRadius" Value="5"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="5"/>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#27AE60"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="30,12"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#229954"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#95A5A6"/>
                    <Setter Property="Cursor" Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="#E74C3C"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#C0392B"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" 
                   Text="Mark Attendance" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   Foreground="#2C3E50"
                   Margin="0,0,0,20"/>

        <!-- Current Status Card -->
        <Border Grid.Row="1" 
                Background="White" 
                BorderBrush="#3498DB" 
                BorderThickness="2"
                CornerRadius="8"
                Padding="25"
                Margin="0,0,0,20">
            <Border.Effect>
                <DropShadowEffect Color="#BDC3C7" BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Today's Date -->
                <StackPanel Grid.Column="0">
                    <TextBlock Text="📅 Today's Date" 
                               FontSize="12" 
                               Foreground="#7F8C8D"
                               Margin="0,0,0,5"/>
                    <TextBlock x:Name="TodayDateText" 
                               Text="Monday, January 01, 2025" 
                               FontSize="16" 
                               FontWeight="SemiBold"
                               Foreground="#2C3E50"/>
                </StackPanel>

                <!-- Check In Time -->
                <StackPanel Grid.Column="1">
                    <TextBlock Text="⏰ Check In Time" 
                               FontSize="12" 
                               Foreground="#7F8C8D"
                               Margin="0,0,0,5"/>
                    <TextBlock x:Name="CheckInTimeText" 
                               Text="--:--" 
                               FontSize="16" 
                               FontWeight="SemiBold"
                               Foreground="#27AE60"/>
                </StackPanel>

                <!-- Check Out Time -->
                <StackPanel Grid.Column="2">
                    <TextBlock Text="🏁 Check Out Time" 
                               FontSize="12" 
                               Foreground="#7F8C8D"
                               Margin="0,0,0,5"/>
                    <TextBlock x:Name="CheckOutTimeText" 
                               Text="--:--" 
                               FontSize="16" 
                               FontWeight="SemiBold"
                               Foreground="#E74C3C"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Action Buttons -->
        <Grid Grid.Row="2" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="CheckInButton" 
                    Grid.Column="0"
                    Content="✓ Check In"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Click="CheckInButton_Click"
                    Height="50"/>

            <Button x:Name="CheckOutButton" 
                    Grid.Column="1"
                    Content="🏁 Check Out"
                    Style="{StaticResource DangerButtonStyle}"
                    Click="CheckOutButton_Click"
                    IsEnabled="False"
                    Height="50"/>
        </Grid>

        <!-- Status Message -->
        <Border Grid.Row="3" 
                x:Name="StatusMessageBorder"
                Style="{StaticResource InfoCardStyle}"
                Visibility="Collapsed">
            <StackPanel>
                <TextBlock x:Name="StatusMessageText" 
                           Text="Status message will appear here" 
                           FontSize="14" 
                           Foreground="#2C3E50"
                           TextWrapping="Wrap"
                           TextAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>